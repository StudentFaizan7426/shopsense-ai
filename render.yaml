services:
  - type: web
    name: shopsense-ai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && gunicorn app:create_app()
    envVars:
      - key: GROQ_API_KEY
        sync: false